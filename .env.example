# Pokemon Card Collection - Docker Environment Variables
# Copy this file to .env and fill in your values

# === REQUIRED ===

# API Key for sync authentication (REQUIRED)
# Generate a strong key with: openssl rand -hex 32
# This key must match the one configured in your CLI:
#   pkm config set web_api_key <same-key-here>
PKMDEX_API_KEY=your-secret-key-here

# === OPTIONAL ===

# Port to expose the web app (default: 8000)
# Change if you have port conflicts
PKMDEX_PORT=8000

# Directory for persistent database storage (default: ./data)
# This directory will be created if it doesn't exist
PKMDEX_DATA_DIR=./data

# === DEPLOYMENT EXAMPLE ===

# For production, use a strong random key:
# PKMDEX_API_KEY=$(openssl rand -hex 32)

# For different port:
# PKMDEX_PORT=3000

# For custom data location:
# PKMDEX_DATA_DIR=/var/lib/pkmdex

# === USAGE ===

# 1. Copy this file:
#    cp .env.example .env

# 2. Edit .env and set your PKMDEX_API_KEY

# 3. Start the container:
#    docker-compose up -d

# 4. Configure CLI to sync:
#    pkm config set web_api_url https://your-domain.com/api/sync
#    pkm config set web_api_key <same-as-PKMDEX_API_KEY>

# 5. Test sync:
#    pkm export --push
